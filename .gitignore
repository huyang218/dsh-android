.DS_Store
build/
.gradle/
local.properties
node_modules/
.playwright-mcp/

# 签名材料:只在本机和 CI 的临时目录里存在,永远不进仓库
keystore.properties
*.jks
*.keystore

# 运行时载荷:75MB 的二进制,由 scripts/prepare-runtime.sh 生成,不进仓库
app/src/main/assets/

# 从设备取来的 Node 树,由 scripts/prepare-runtime.sh --from-device 生成
vendor/
